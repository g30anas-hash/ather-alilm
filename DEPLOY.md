# دليل نشر مشروع "أثير العلم" (Ather Alilm)

هذا الدليل يشرح خطوات نشر الموقع على الإنترنت باستخدام خدمة **Vercel** المجانية، وربطه بقاعدة بيانات **Supabase**.

## المتطلبات المسبقة

1.  حساب على [GitHub](https://github.com).
2.  حساب على [Vercel](https://vercel.com).
3.  مشروعك على [Supabase](https://supabase.com) (جاهز بالفعل).

---

## الخطوة 1: رفع الكود إلى GitHub

بما أنك تستخدم بيئة تطوير (IDE)، يمكنك استخدام واجهة "التحكم في المصدر" (Source Control) لرفع الكود، أو استخدام الأوامر التالية إذا كان `git` مثبتاً لديك:

1.  افتح نافذة الأوامر (Terminal) في مجلد المشروع.
2.  نفذ الأوامر التالية بالترتيب:

```bash
# تهيئة المستودع (إذا لم يكن مهيأ)
git init

# إضافة جميع الملفات
git add .

# حفظ التغييرات
git commit -m "Initial commit - Ather Alilm Project"

# (استبدل الرابط أدناه برابط مستودعك الجديد على GitHub)
git remote add origin https://github.com/USERNAME/ather-alilm.git
git branch -M main
git push -u origin main
```

*ملاحظة: إذا واجهت مشكلة في الأوامر، استخدم زر "Publish to GitHub" الموجود في واجهة البرنامج.*

---

## الخطوة 2: النشر على Vercel

1.  اذهب إلى لوحة تحكم [Vercel](https://vercel.com/dashboard).
2.  اضغط على زر **Add New...** ثم اختر **Project**.
3.  اختر **Import** بجانب مستودع `ather-alilm` الذي رفعته للتو.
4.  في صفحة الإعدادات (Configure Project):
    *   اترك إعدادات البناء (Build Settings) كما هي.
    *   افتح قسم **Environment Variables**.
    *   أضف المتغيرات التالية (انسخ القيم من ملف `.env.local` الموجود في جهازك):

    | الاسم (Key) | القيمة (Value) |
    | :--- | :--- |
    | `NEXT_PUBLIC_SUPABASE_URL` | `https://cwhubuhcueavscvmtxkh.supabase.co` |
    | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | *(انسخ المفتاح الطويل جداً من ملف .env.local)* |

5.  اضغط على زر **Deploy**.

---

## الخطوة 3: تهانينا! 🎉

بعد انتهاء عملية البناء (تستغرق حوالي دقيقة)، ستحصل على رابط دائم لموقعك (مثل `ather-alilm.vercel.app`).

يمكنك مشاركة هذا الرابط مع الطلاب والمعلمين، وسيعمل النظام بشكل كامل مع قاعدة البيانات السحابية.
